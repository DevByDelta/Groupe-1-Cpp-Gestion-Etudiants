#pragma once
// Classe.hpp  --  généré automatiquement
// Kiné

class Classe {
private:
    std::string id;
    std::string nom;
    int idFormation;
    std::vector <std::string>etudiantCodes;
public:
    Classe();
    ~Classe();
};
